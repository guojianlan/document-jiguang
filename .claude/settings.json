{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.svg) cd /Users/apple/Desktop/project/document && node scripts/check_svg_layout.js \"$f\" ;; esac; }",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
